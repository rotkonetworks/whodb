export const HelpCarousel = ({ className }: {
  className?: string;
  autoPlayInterval?: number;
  currentSlideIndex?: number;
  onSlideIndexChange?: (index: number) => void;
}) => {
  return (
    <div className={className}>
      {/* TODO: Implement help carousel */}
    </div>
  );
};
